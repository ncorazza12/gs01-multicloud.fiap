apiVersion: v1
kind: Service
metadata:
  name: gs01-api
  namespace: demo
  labels:
    app: gs01-api
spec:
  type: LoadBalancer
  selector:
    app: gs01-api
  ports:
    - name: http
      port: 80
      targetPort: 3000
